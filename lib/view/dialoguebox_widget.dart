import 'package:flutter/material.dart';
import 'package:get/get.dart';

class DialogueboxWidget extends StatelessWidget {
  const DialogueboxWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
